on:
  pull_request:
    types: [opened, edited]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check action step
        id: checkstep
        uses: tokghwt/my-js-action@v0.8.0
        with:
          pattern: '^(?:feat|fix|release|chore)(?:\([^)]+\))?: [A-Z]'
